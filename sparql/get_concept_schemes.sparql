PREFIX skos:<http://www.w3.org/2004/02/skos/core#>
PREFIX dcterms:<http://purl.org/dc/terms/>
CONSTRUCT { 
?node dcterms:title ?title .
} 
WHERE { 
?node a skos:ConceptScheme ;
dcterms:title ?title .
{{if .Language}}FILTER(LANGMATCHES(LANG(?title), "{{.Language}}")){{end}}
}
